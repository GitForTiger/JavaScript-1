"use strict" //Treat all the JS code as per the new version
/*
alert(3+3) 
Node.js cannot run alert() because alert() is a browser function, not a JS function.
*/
let name = "Apple"
let age = 18
let isLoggedIn = false
let state;

// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => standalone value
// undefined => 
// symbol => unique


// object

console.log(typeof undefined); // undefined
console.log(typeof null); // object